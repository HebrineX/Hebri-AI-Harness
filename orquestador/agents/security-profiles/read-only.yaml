schema: hebrinex.agent_security_profile
version: "0.1"
id: read-only
filesystem:
  read: declared_only
  write: deny
commands:
  allowed_risk_classes: [read_only]
network: deny
secrets: deny
git_remote: deny
escalation: deny
