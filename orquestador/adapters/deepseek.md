# Deepseek Adapter

Contrato base compartido: leer `orquestador/adapters/_shared-core.md` (entrada minima 0.13.1, presupuesto en `orquestador/context-budget.yaml`, preflight + `SI` antes de efectos).

Notas especificas: estado experimental; soporte de hooks no investigado (`unknown` en `deepseek.yaml`); instrucciones via prompt del host o `AGENTS.md`.
