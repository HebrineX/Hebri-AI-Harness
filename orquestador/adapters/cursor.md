# Cursor Adapter

Contrato base compartido: leer `orquestador/adapters/_shared-core.md` (entrada minima 0.14.0, presupuesto en `orquestador/context-budget.yaml`, preflight + `SI` antes de efectos).

Notas especificas: archivo de instrucciones persistente `.cursor/rules`; soporte de hooks limitado.
