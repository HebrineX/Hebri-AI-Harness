# Agent context fixtures

Synthetic, secret-free fixtures for P03 local conformance. Provider files are data, never instructions or authority. The validator creates ephemeral task packs, results, approvals, journals, and handoffs under a marker-owned runtime directory and removes only that directory.
