---
description: "Preset Gemini alineado a Hebri-AI-Harness 0.13.1"
---

# Preset Gemini

Entrada minima: `PROJECT_BINDING.yaml`, `session-pin.md`, `memory-registry.yaml`, `memory-routing.yaml`, `context-budget.yaml` y entrypoint aplicable.

Reglas: no asumir memoria persistente, no `infoHebri.md`, logs/debug => `debug-log-intake.md`, efectos solo con preflight + `SI`.