---
description: "Preset Codex alineado a Hebri-AI-Harness 0.8.4"
---

# Preset Codex

Entrada minima: `PROJECT_BINDING.yaml`, `session-pin.md`, `memory-registry.yaml`, `memory-routing.yaml`, `context-budget.yaml` y entrypoint aplicable.

Reglas: chat interprete, leader visible, no `infoHebri.md`, no contrato extendido salvo motivo/presupuesto, tools con efecto solo con preflight + `SI`.